# Configuración de Jobs para orgmos
# Este archivo define las tareas automatizadas que se ejecutarán como cronjobs

# URL base para healthchecks
healthcheck_base_url: "https://hc.or-gm.com"

# Webhook de Discord (opcional, para uso futuro)
discord_webhook: ""

# Tareas de sincronización
sync_tasks:
  # Ejemplo de tarea de sincronización
  # proyectos:
  #   source: "root@servidor:/ruta/origen/"
  #   dest: "/home/usuario/destino/"
  #   healthcheck_id: "uuid-del-healthcheck"
  #   rsync_args: ["-avz", "-e", "ssh"]
  #   crontab_schedule: "0 2 * * *"  # Ejecutar diariamente a las 2:00 AM
  #                                  # Formato: "minuto hora día_mes mes día_semana"
  #                                  # Ejemplos:
  #                                  #   "0 2 * * *"     - Diario a las 2:00 AM
  #                                  #   "0 */6 * * *"   - Cada 6 horas
  #                                  #   "0 0 * * 0"     - Cada domingo a medianoche
  #                                  #   "30 3 1 * *"    - El día 1 de cada mes a las 3:30 AM
  
  # Agrega tus tareas aquí:
  # nombre_tarea:
  #   source: "usuario@servidor:/ruta/origen/"
  #   dest: "/ruta/destino/local/"
  #   healthcheck_id: "tu-uuid-aqui"
  #   rsync_args: ["-avz", "-e", "ssh"]
  #   crontab_schedule: "0 2 * * *"


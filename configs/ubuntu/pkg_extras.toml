# Paquetes extras para Ubuntu

["GIT Y DOCKER UI"]
packages = [
    # lazygit: instalación desde PPA o go install
    # lazydocker: instalación manual desde releases o go install
    # uv: pip install uv o instalación desde pipx
]

["UTILIDADES"]
packages = [
    "smbclient",          # cliente SMB/CIFS
    "tmux",               # terminal multiplexer
    # ctop: instalación manual desde releases
    "rsync",              # sincronización de archivos
    # watchexec: cargo install watchexec-cli
    "nnn",                # explorador de archivos
]

["MONITORES Y SISTEMA"]
packages = [
    # pacseek: solo disponible en Arch
    # peaclock: instalación manual desde releases
]

